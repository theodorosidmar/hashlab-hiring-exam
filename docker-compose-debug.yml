version: '3'
services:
  products-service:
    build:
      context: ./products-service
      dockerfile: Dockerfile.debug
    environment:
      - PORT=3000
      - DISCOUNTS_SERVICE_HOST=localhost
      - DISCOUNTS_SERVICE_PORT=3001
    container_name: products-service-debug
    image: products-service-debug:latest
    restart: always
    tty: true
    ports:
      - 3000:3000
      - 9229:9229
  discounts-service:
    build:
      context: ./discounts-service
      dockerfile: Dockerfile
    environment:
      - HOST=localhost
      - PORT=3001
    container_name: discounts-service
    image: discounts-service:latest
    restart: always
    tty: true
    ports:
      - 3001:3001